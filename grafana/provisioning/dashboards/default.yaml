apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards  # Ce chemin doit correspondre à /etc/grafana/provisioning/dashboards dans le conteneur
